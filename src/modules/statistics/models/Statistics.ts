export interface Statistics {
    id: number;
    user_id: number;
    competitions_completed: number;
    total_score: number;
    average_time_per_competition: number;
    average_score_per_competition: number;
    average_time_per_challenge:number;
    best_score: number;
    challenge_completed_count: number;
}